function convert(str){
    let result=""
    let word =""
    for(let i= 0; i<=str.length;i++){
        let word= str[i]
        for(let j=0; j<=word.length;j++){
            let ch=word[i]
            let wordresult =""
        }
        if(ch == word[0]&& ch== word.length-2){
            wordresult= wordresult.fromCharCode(charCodeAt(0)-32)
    }
    else{
        result=word[j]
    }
}
}